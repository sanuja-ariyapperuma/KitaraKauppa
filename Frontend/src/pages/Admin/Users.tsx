import Grid from "@mui/material/Grid2";
import { CSSProperties } from "react";

const Users = () => {
  const containerStyle: CSSProperties = {
    marginLeft: "400px",
    marginTop: "20px",
  };

  return <Grid style={containerStyle}>Users</Grid>;
};

export default Users;
